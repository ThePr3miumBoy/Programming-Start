#include <stdio.h>
int main()
{   
    int xyz = 256;
    printf("\nThe value of xyz is: %d",xyz);
    xyz = 156;
    printf("\nThe new value of xyz is: %d",xyz);

    //use of consonant 
    const float vvdx = 21.25; // constant is used to fix variables value
    printf("\nThe value of float is: %.2f",vvdx);

    return 0;
}